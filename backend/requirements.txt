fastapi==0.115.0
uvicorn[standard]==0.30.3
httpx==0.27.0
python-dotenv==1.0.1
websockets==13.1
pydantic==2.9.2

# AWS SDK for DynamoDB (optional, only needed if using DynamoDB storage)
boto3==1.35.0

# RAG dependencies
chromadb==0.4.22
beautifulsoup4==4.12.3

# Minimal requirements for Mistral via API
# No local transformers/torch dependencies required when using remote API
